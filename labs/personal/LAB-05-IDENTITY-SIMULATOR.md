# Lab 05 — Identity Operations Simulator

Create synthetic users/groups.

Command:

> Remove Alex Example from all access governed by the termination policy.

The agent must:
- resolve identity;
- retrieve memberships;
- apply deterministic policy;
- show plan;
- get approval;
- execute against synthetic data;
- verify result.

Do not connect to employer identity systems.
