# Runtime Golden Evals

Run the reference runtime unit tests first:

```bash
python -m unittest discover -s reference_runtime/tests -v
```

These eval definitions describe expected behavior for future real adapters and
LLM-based specialists.
