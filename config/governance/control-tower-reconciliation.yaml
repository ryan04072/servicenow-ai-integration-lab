version: 1
reconciliation_fields:
  - ai_control_tower_asset_id
  - capability_id
  - runtime_platform
  - runtime_asset_id
  - repository
  - work_item
  - ai_asset_owner
  - technical_owner
  - risk_tier
  - lifecycle_state
  - environment
discovered_asset_actions:
  known: {action: reconcile_to_existing_asset}
  duplicate: {action: link_and_consolidate_review}
  approved_candidate: {action: steward_review_for_managed_lifecycle}
  unauthorized_or_high_risk: {action: investigate_and_contain_per_policy}
  retirement_candidate: {action: begin_offboarding_review}
